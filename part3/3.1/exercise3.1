Here is a link to the example repository that contains the workflow-file as well as the docker-compose.yml with both the example app and the watchtower in it. I tested that the pipeline works.
https://github.com/maaksuw/example-node-app
